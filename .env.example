# GitHub OAuth Configuration
GITHUB_CLIENT_ID=your_github_client_id_here
GITHUB_CLIENT_SECRET=your_github_client_secret_here

# JWT Keys (RS256 - generate with openssl)
# openssl genrsa -out private.pem 2048
# openssl rsa -in private.pem -pubout -out public.pem
JWT_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\nYOUR_PRIVATE_KEY_HERE\n-----END PRIVATE KEY-----"
JWT_PUBLIC_KEY="-----BEGIN PUBLIC KEY-----\nYOUR_PUBLIC_KEY_HERE\n-----END PUBLIC KEY-----"

# Refresh Token Encryption (AES-256 key)
# openssl rand -base64 32
REFRESH_ENCRYPTION_KEY=your_32_byte_base64_encoded_key_here

# Worker Base URL (your deployed worker URL)
WORKER_BASE_URL=https://your-worker.your-subdomain.workers.dev